const houseToUserService = require('../services/house_user.service');

const houseToUserController = {

    add: async(req, res) => {

        res.sendStatus(501); // TODO à faire !

    }
};

module.exports = {
    houseToUserController
}