const db = require('../models');
const { HouseToUser } = require('../models/house_user.model');

const houseToUserService = {

    add: async(userId) => {

        //TODO faire la fonction
        //return new HouseToUser(hasAdmin);

    }

};

module.exports = {
    houseToUserService
}